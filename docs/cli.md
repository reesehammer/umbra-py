# CLI reference

Every `umbra` command and flag below is generated directly from the installed
Click application, so it always matches `umbra --help`.

```bash
umbra --help
```

::: mkdocs-click
    :module: umbra_py.cli
    :command: cli
    :prog_name: umbra
    :depth: 1
    :style: table
